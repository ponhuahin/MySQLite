<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.mogkolpon.mysqlite">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Popo" />
        <activity android:name=".Admin_SingnUp" />
        <activity android:name=".AdminEdit" />
        <activity android:name=".AdminShow" />
        <activity android:name=".NameRegister" />
        <activity android:name=".NameShow" />
        <activity android:name=".NameEdit"></activity>
    </application>

</manifest>